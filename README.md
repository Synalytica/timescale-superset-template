# TimeScaleDB (+ Apache Superset for visualization)


## Introduction

Basic setup for financial data visualization and analysis.

## Installation

```bash
cp -rv env.example .env  # populate with values
docker-compose up --build -d
```

